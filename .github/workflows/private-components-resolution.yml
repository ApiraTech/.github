name: Private components fork-resolution probe

on:
  pull_request:

permissions: {}

jobs:
  resolve-private-component:
    name: Resolve organization-shared private workflow
    permissions:
      checks: read
      contents: write
      pull-requests: write
    uses: ApiraTech/cicd-components/.github/workflows/dependabot-auto-merge.yml@v1
    with:
      dry-run: true
